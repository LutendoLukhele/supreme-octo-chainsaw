{
  "tools": [
    {
      "name": "request_missing_parameters",
      "description": "Use this tool ONLY when required parameters for another tool are missing and cannot be reasonably defaulted. Provide the intended tool and the list of missing required parameter names.",
      "providerConfigKey": "__META__",
      "category": "Meta",
      "parameters": {
        "type": "object",
        "properties": {
          "input": {
            "type": "object",
            "properties": {
              "intended_tool_name": {
                "type": "string",
                "description": "The exact name of the tool the user likely intended to use (e.g., 'fetch_entity')."
              },
              "missing_params": {
                "type": "array",
                "items": { "type": "string" },
                "description": "A list of the exact names of REQUIRED parameters that are missing for the 'intended_tool_name'."
              },
              "clarification_question": {
                "type": "string",
                "description": "A clear question to ask the user to obtain the missing parameters, phrased from the assistant's perspective."
              }
            },
            "required": [
              "intended_tool_name",
              "missing_params",
              "clarification_question"
            ]
          }
        },
        "required": ["input"]
      }
    },
    {
      "name": "fetch_emails",
      "description": "Fetch Gmail emails with advanced filtering options based on criteria like sender, subject, date, labels, or attachments.",
      "source": "cache",
      "cache_model": "GmailThread",
      "providerConfigKey": "google-mail-ynxw",
      "category": "Email",
      "parameters": {
        "type": "object",
        "properties": {
          "input": {
            "type": "object",
            "properties": {
              "operation": {
                "type": "string",
                "enum": ["fetch"],
                "description": "Specifies the operation to perform, always 'fetch' for this tool."
              },
              "backfillPeriodMs": {
                "type": "integer",
                "description": "Optional: How far back in time to search for emails, specified in milliseconds."
              },
              "filters": {
                "type": "object",
                "description": "Optional: A set of criteria to filter the emails.",
                "properties": {
                  "sender": {
                    "type": "string",
                    "description": "Filter by the sender's email address."
                  },
                  "recipient": {
                    "type": "string",
                    "description": "Filter by the recipient's email address."
                  },
                  "subject": {
                    "type": "object",
                    "description": "Filter by keywords or phrases in the email subject.",
                    "properties": {
                      "contains": { "type": "array", "items": { "type": "string" }, "description": "Subject must contain ALL these keywords." },
                      "startsWith": { "type": "string", "description": "Subject must start with this text." },
                      "endsWith": { "type": "string", "description": "Subject must end with this text." },
                      "exact": { "type": "string", "description": "Subject must match exactly." }
                    }
                  },
                  "dateRange": {
                    "type": "object",
                    "description": "Filter emails received within a specific date range.",
                    "properties": {
                      "after": { "type": "string", "description": "Include emails received after this ISO 8601 timestamp." },
                      "before": { "type": "string", "description": "Include emails received before this ISO 8601 timestamp." }
                    }
                  },
                  "hasAttachment": { "type": "boolean", "description": "Filter for emails that have attachments." },
                  "labels": { "type": "array", "items": { "type": "string" }, "description": "Filter by Gmail labels (e.g., INBOX, IMPORTANT, or custom labels)." },
                  "includeBody": { "type": "boolean", "description": "Whether to include the full email body in the result (can be large). Defaults to false." },
                  "excludeCategories": { "type": "array", "items": { "type": "string" }, "description": "Gmail categories to exclude (e.g., Promotions, Social, Updates)." },
                  "isRead": { "type": "boolean", "description": "Filter based on read/unread status (true for read, false for unread)." },
                  "isImportant": { "type": "boolean", "description": "Filter for emails marked as important by Gmail." },
                  "includeSpam": { "type": "boolean", "description": "Whether to include emails from the Spam folder. Defaults to false." },
                  "includeTrash": { "type": "boolean", "description": "Whether to include emails from the Trash folder. Defaults to false." },
                  "semanticType": { "type": "string", "enum": ["security", "billing", "calendar", "support", "promotion", "general"], "description": "Filter by automatically detected email intent type. Use 'security' for alerts, 'billing' for invoices/payments, 'calendar' for meeting invites, 'support' for help tickets, 'promotion' for marketing emails." },
                  "minConfidence": { "type": "number", "minimum": 0, "maximum": 1, "description": "Minimum semantic classification confidence (0.0-1.0). Use with semanticType to filter only high-confidence matches. Example: 0.8 for high confidence." },
                  "limit": { "type": "integer", "description": "Maximum number of emails to return (default: 15, max: 15).", "default": 15, "maximum": 15 },
                  "offset": { "type": "integer", "description": "Number of emails to skip (for pagination)." }
                }
              }
            },
            "required": ["operation"] 
          }
        },
        "required": ["input"]
      }
    },
    {
      "name": "send_email",
      "description": "Send an email via Gmail.",
      "source": "action",
      "providerConfigKey": "google-mail-ynxw",
      "category": "Email",
      "parameters": {
        "type": "object",
        "properties": {
          "input": {
            "type": "object",
            "properties": {
              "operation": { "type": "string", "enum": ["send"], "description": "Operation must be 'send'." },
              "to": { "type": "string", "description": "The recipient's email address." },
              "subject": { "type": "string", "description": "The subject line of the email." },
              "body": { "type": "string", "description": "The main content/body of the email." }
            },
            "required": ["operation", "to", "subject", "body"]
          }
        },
        "required": ["input"]
      }
    },
    {
      "name": "fetch_calendar_events",
      "description": "Fetch Google Calendar events. Use operation 'fetch' for a single event by ID, or 'fetch_list' to query multiple events with date range filters and search.",
      "source": "cache",
      "cache_model": "CalendarEvent",
      "providerConfigKey": "google-calendar",
      "category": "Calendar",
      "parameters": {
        "type": "object",
        "properties": {
          "input": {
            "type": "object",
            "properties": {
              "operation": {
                "type": "string",
                "enum": ["fetch", "fetch_list"],
                "description": "Use 'fetch' to get a single event by ID, or 'fetch_list' to query multiple events."
              },
              "calendarId": {
                "type": "string",
                "description": "The ID of the calendar. Defaults to 'primary'.",
                "default": "primary"
              },
              "eventId": {
                "type": "string",
                "description": "Required for operation 'fetch': The ID of the specific event to retrieve."
              },
              "filters": {
                "type": "object",
                "description": "Optional filters for operation 'fetch_list' to query multiple events.",
                "properties": {
                  "dateRange": {
                    "type": "object",
                    "description": "Filter events by time range.",
                    "properties": {
                      "timeMin": {
                        "type": "string",
                        "description": "Start of time range (RFC3339 format, e.g., '2024-11-01T00:00:00Z')."
                      },
                      "timeMax": {
                        "type": "string",
                        "description": "End of time range (RFC3339 format, e.g., '2024-11-30T23:59:59Z')."
                      }
                    }
                  },
                  "conditions": {
                    "type": "array",
                    "items": {
                      "type": "object",
                      "properties": {
                        "field": {
                          "type": "string",
                          "enum": ["summary", "description", "location", "attendee", "creator", "organizer"],
                          "description": "Field to filter on."
                        },
                        "operator": {
                          "type": "string",
                          "enum": ["contains", "equals", "starts_with", "ends_with"],
                          "description": "Filter operator."
                        },
                        "value": {
                          "type": "string",
                          "description": "Value to filter by."
                        }
                      },
                      "required": ["field", "operator", "value"]
                    },
                    "description": "Additional filter conditions on event fields."
                  },
                  "showDeleted": {
                    "type": "boolean",
                    "description": "Include deleted events? Defaults to false."
                  },
                  "singleEvents": {
                    "type": "boolean",
                    "description": "Expand recurring events into instances? Defaults to true.",
                    "default": true
                  },
                  "orderBy": {
                    "type": "string",
                    "enum": ["startTime", "updated"],
                    "description": "Sort results by start time or last updated. Requires singleEvents=true for 'startTime'."
                  },
                  "limit": {
                    "type": "integer",
                    "description": "Maximum number of events to return (max 2500)."
                  },
                  "q": {
                    "type": "string",
                    "description": "Free text search query to find events."
                  },
                  "iCalUID": {
                    "type": "string",
                    "description": "Filter by specific iCalendar UID."
                  },
                  "updatedMin": {
                    "type": "string",
                    "description": "Filter events modified after this timestamp."
                  }
                }
              },
              "format": {
                "type": "string",
                "enum": ["detailed", "simple", "raw"],
                "description": "Response format: 'detailed' (normalized), 'simple' (minimal), or 'raw' (API response).",
                "default": "detailed"
              }
            },
            "required": ["operation"]
          }
        },
        "required": ["input"]
      }
    },
    {
      "name": "create_calendar_event",
      "description": "Create a new event, meeting, or appointment in Google Calendar with full support for attendees, reminders, recurrence, and conference data.",
      "source": "action",
      "providerConfigKey": "google-calendar",
      "category": "Calendar",
      "parameters": {
        "type": "object",
        "properties": {
          "input": {
            "type": "object",
            "properties": {
              "operation": {
                "type": "string",
                "enum": ["create", "batch_create"],
                "description": "Use 'create' for a single event or 'batch_create' for multiple events."
              },
              "calendarId": {
                "type": "string",
                "description": "The ID of the calendar. Defaults to 'primary'.",
                "default": "primary"
              },
              "event": {
                "type": "object",
                "description": "Event details for operation 'create'.",
                "properties": {
                  "summary": {
                    "type": "string",
                    "description": "The title/summary of the event."
                  },
                  "description": {
                    "type": "string",
                    "description": "Detailed description of the event."
                  },
                  "location": {
                    "type": "string",
                    "description": "Physical or virtual location of the event."
                  },
                  "start": {
                    "type": "object",
                    "description": "Start time of the event.",
                    "properties": {
                      "dateTime": {
                        "type": "string",
                        "description": "For timed events (RFC3339 format, e.g., '2024-11-20T10:00:00-08:00')."
                      },
                      "date": {
                        "type": "string",
                        "description": "For all-day events (YYYY-MM-DD format)."
                      },
                      "timeZone": {
                        "type": "string",
                        "description": "Time zone (e.g., 'America/Los_Angeles')."
                      }
                    }
                  },
                  "end": {
                    "type": "object",
                    "description": "End time of the event.",
                    "properties": {
                      "dateTime": {
                        "type": "string",
                        "description": "For timed events."
                      },
                      "date": {
                        "type": "string",
                        "description": "For all-day events."
                      },
                      "timeZone": {
                        "type": "string",
                        "description": "Time zone."
                      }
                    }
                  },
                  "attendees": {
                    "type": "array",
                    "items": {
                      "type": "object",
                      "properties": {
                        "email": { "type": "string", "description": "Attendee email address." },
                        "displayName": { "type": "string" },
                        "optional": { "type": "boolean", "description": "Is this attendee optional?" }
                      },
                      "required": ["email"]
                    },
                    "description": "List of event attendees."
                  },
                  "reminders": {
                    "type": "object",
                    "description": "Event reminders configuration.",
                    "properties": {
                      "useDefault": {
                        "type": "boolean",
                        "description": "Use calendar default reminders?"
                      },
                      "overrides": {
                        "type": "array",
                        "items": {
                          "type": "object",
                          "properties": {
                            "method": { "type": "string", "enum": ["email", "popup", "sms"] },
                            "minutes": { "type": "integer", "description": "Minutes before event." }
                          },
                          "required": ["method", "minutes"]
                        }
                      }
                    }
                  },
                  "recurrence": {
                    "type": "array",
                    "items": { "type": "string" },
                    "description": "Recurrence rules in RRULE format (e.g., ['RRULE:FREQ=WEEKLY;COUNT=10'])."
                  },
                  "conferenceData": {
                    "type": "object",
                    "description": "Conference/meeting link configuration (e.g., Google Meet).",
                    "properties": {
                      "createRequest": {
                        "type": "object",
                        "properties": {
                          "requestId": { "type": "string", "description": "Unique request ID." },
                          "conferenceSolutionKey": {
                            "type": "object",
                            "properties": {
                              "type": { "type": "string", "enum": ["hangoutsMeet", "eventHangout"] }
                            }
                          }
                        }
                      }
                    }
                  },
                  "visibility": {
                    "type": "string",
                    "enum": ["default", "public", "private", "confidential"]
                  },
                  "transparency": {
                    "type": "string",
                    "enum": ["opaque", "transparent"],
                    "description": "'opaque' = busy, 'transparent' = free."
                  }
                },
                "required": ["summary", "start", "end"]
              },
              "events": {
                "type": "array",
                "items": {
                  "type": "object",
                  "description": "Event object (same structure as 'event' above)."
                },
                "description": "Array of events for operation 'batch_create' (max 50)."
              },
              "sendUpdates": {
                "type": "string",
                "enum": ["all", "externalOnly", "none"],
                "description": "Send calendar invitations to attendees? Defaults to 'none'.",
                "default": "none"
              },
              "conferenceDataVersion": {
                "type": "integer",
                "enum": [0, 1],
                "description": "Set to 1 to enable conference data creation."
              }
            },
            "required": ["operation"]
          }
        },
        "required": ["input"]
      }
    },
    {
      "name": "update_calendar_event",
      "description": "Update existing Google Calendar event(s). Use 'update' for a single event or 'batch_update' for multiple events.",
      "source": "action",
      "providerConfigKey": "google-calendar",
      "category": "Calendar",
      "parameters": {
        "type": "object",
        "properties": {
          "input": {
            "type": "object",
            "properties": {
              "operation": {
                "type": "string",
                "enum": ["update", "batch_update"],
                "description": "Use 'update' for single event, 'batch_update' for multiple."
              },
              "calendarId": {
                "type": "string",
                "description": "The ID of the calendar. Defaults to 'primary'.",
                "default": "primary"
              },
              "eventId": {
                "type": "string",
                "description": "Required for 'update': The ID of the event to update."
              },
              "fields": {
                "type": "object",
                "description": "Fields to update for operation 'update'. Only provided fields will be changed.",
                "properties": {
                  "summary": { "type": "string" },
                  "description": { "type": "string" },
                  "location": { "type": "string" },
                  "start": {
                    "type": "object",
                    "properties": {
                      "dateTime": { "type": "string" },
                      "date": { "type": "string" },
                      "timeZone": { "type": "string" }
                    }
                  },
                  "end": {
                    "type": "object",
                    "properties": {
                      "dateTime": { "type": "string" },
                      "date": { "type": "string" },
                      "timeZone": { "type": "string" }
                    }
                  },
                  "attendees": { "type": "array" },
                  "reminders": { "type": "object" },
                  "status": {
                    "type": "string",
                    "enum": ["confirmed", "tentative", "cancelled"]
                  }
                }
              },
              "updates": {
                "type": "array",
                "items": {
                  "type": "object",
                  "properties": {
                    "eventId": { "type": "string", "description": "Event ID to update." },
                    "fields": { "type": "object", "description": "Fields to update." }
                  },
                  "required": ["eventId", "fields"]
                },
                "description": "Array of updates for operation 'batch_update' (max 50)."
              },
              "sendUpdates": {
                "type": "string",
                "enum": ["all", "externalOnly", "none"],
                "description": "Notify attendees of changes? Defaults to 'none'.",
                "default": "none"
              }
            },
            "required": ["operation"]
          }
        },
        "required": ["input"]
      }
    },
    {
      "name": "fetch_notion_page",
      "description": "Fetch Notion pages or query database rows. Use operation 'fetch' for a single page by ID, or 'fetch_database' to query a database with filters and sorting.",
      "source": "cache",
      "cache_model": "NotionPage",
      "providerConfigKey": "notion",
      "category": "Productivity",
      "parameters": {
        "type": "object",
        "properties": {
          "input": {
            "type": "object",
            "properties": {
              "operation": {
                "type": "string",
                "enum": ["fetch", "fetch_database"],
                "description": "Use 'fetch' for a single page, 'fetch_database' to query database rows."
              },
              "entityType": {
                "type": "string",
                "enum": ["Page", "DatabaseRow"],
                "description": "Type of entity to fetch."
              },
              "identifier": {
                "type": "string",
                "description": "Required for 'fetch': The page ID (UUID format)."
              },
              "databaseId": {
                "type": "string",
                "description": "Required for 'fetch_database': The database ID (UUID format)."
              },
              "filters": {
                "type": "object",
                "description": "Filters for operation 'fetch_database'.",
                "properties": {
                  "conditions": {
                    "type": "array",
                    "items": {
                      "type": "object",
                      "properties": {
                        "property": {
                          "type": "string",
                          "description": "Property name to filter on."
                        },
                        "operator": {
                          "type": "string",
                          "enum": [
                            "equals", "does_not_equal", "contains", "does_not_contain",
                            "starts_with", "ends_with", "is_empty", "is_not_empty",
                            "greater_than", "less_than", "greater_than_or_equal_to", "less_than_or_equal_to",
                            "before", "after", "on_or_before", "on_or_after"
                          ],
                          "description": "Filter operator."
                        },
                        "value": {
                          "description": "Value to filter by."
                        }
                      },
                      "required": ["property", "operator"]
                    },
                    "description": "List of filter conditions."
                  },
                  "logic": {
                    "type": "string",
                    "enum": ["and", "or"],
                    "description": "Logical operator for combining conditions."
                  },
                  "sorts": {
                    "type": "array",
                    "items": {
                      "type": "object",
                      "properties": {
                        "property": { "type": "string" },
                        "timestamp": {
                          "type": "string",
                          "enum": ["created_time", "last_edited_time"]
                        },
                        "direction": {
                          "type": "string",
                          "enum": ["ascending", "descending"]
                        }
                      },
                      "required": ["direction"]
                    },
                    "description": "Sorting configuration."
                  },
                  "limit": {
                    "type": "integer",
                    "description": "Maximum number of results (max 100)."
                  },
                  "includeArchived": {
                    "type": "boolean",
                    "description": "Include archived pages?"
                  }
                }
              },
              "format": {
                "type": "string",
                "enum": ["detailed", "simple", "raw"],
                "description": "Response format. Defaults to 'detailed'.",
                "default": "detailed"
              }
            },
            "required": ["operation"]
          }
        },
        "required": ["input"]
      }
    },
    {
      "name": "create_notion_page",
      "description": "Create new Notion pages in a database or as child pages. Use 'create' for a single page or 'batch_create' for multiple pages.",
      "source": "action",
      "providerConfigKey": "notion",
      "category": "Productivity",
      "parameters": {
        "type": "object",
        "properties": {
          "input": {
            "type": "object",
            "properties": {
              "operation": {
                "type": "string",
                "enum": ["create", "batch_create"],
                "description": "Use 'create' for single page, 'batch_create' for multiple."
              },
              "parent": {
                "type": "object",
                "description": "Parent location for operation 'create'.",
                "properties": {
                  "type": {
                    "type": "string",
                    "enum": ["database_id", "page_id", "workspace"]
                  },
                  "database_id": { "type": "string" },
                  "page_id": { "type": "string" },
                  "workspace": { "type": "boolean" }
                },
                "required": ["type"]
              },
              "properties": {
                "type": "object",
                "description": "Page properties (key-value pairs). Values can be strings, numbers, booleans, or Notion property objects."
              },
              "children": {
                "type": "array",
                "items": {
                  "type": "object",
                  "description": "Block objects for page content."
                },
                "description": "Content blocks to add to the page."
              },
              "icon": {
                "type": "object",
                "description": "Page icon (emoji or image).",
                "properties": {
                  "type": { "type": "string", "enum": ["emoji", "external", "file"] },
                  "emoji": { "type": "string" },
                  "external": { "type": "object", "properties": { "url": { "type": "string" } } }
                }
              },
              "cover": {
                "type": "object",
                "description": "Page cover image.",
                "properties": {
                  "type": { "type": "string", "enum": ["external", "file"] },
                  "external": { "type": "object", "properties": { "url": { "type": "string" } } }
                }
              },
              "pages": {
                "type": "array",
                "items": {
                  "type": "object",
                  "description": "Page object (same structure as single create)."
                },
                "description": "Array of pages for 'batch_create' (max 100)."
              }
            },
            "required": ["operation"]
          }
        },
        "required": ["input"]
      }
    },
    {
      "name": "update_notion_page",
      "description": "Update existing Notion page(s) properties, archive status, icon, or cover. Use 'update' for a single page or 'batch_update' for multiple.",
      "source": "action",
      "providerConfigKey": "notion",
      "category": "Productivity",
      "parameters": {
        "type": "object",
        "properties": {
          "input": {
            "type": "object",
            "properties": {
              "operation": {
                "type": "string",
                "enum": ["update", "batch_update"],
                "description": "Use 'update' for single page, 'batch_update' for multiple."
              },
              "pageId": {
                "type": "string",
                "description": "Required for 'update': The page ID to update."
              },
              "properties": {
                "type": "object",
                "description": "Properties to update (only specified properties will change)."
              },
              "archived": {
                "type": "boolean",
                "description": "Archive (true) or restore (false) the page."
              },
              "icon": {
                "type": "object",
                "description": "Update page icon.",
                "properties": {
                  "type": { "type": "string", "enum": ["emoji", "external", "file"] },
                  "emoji": { "type": "string" },
                  "external": { "type": "object" }
                }
              },
              "cover": {
                "type": "object",
                "description": "Update page cover.",
                "properties": {
                  "type": { "type": "string", "enum": ["external", "file"] },
                  "external": { "type": "object" }
                }
              },
              "updates": {
                "type": "array",
                "items": {
                  "type": "object",
                  "properties": {
                    "pageId": { "type": "string" },
                    "properties": { "type": "object" },
                    "archived": { "type": "boolean" },
                    "icon": { "type": "object" },
                    "cover": { "type": "object" }
                  },
                  "required": ["pageId"]
                },
                "description": "Array of updates for 'batch_update' (max 100)."
              }
            },
            "required": ["operation"]
          }
        },
        "required": ["input"]
      }
    },
    {
      "name": "fetch_entity",
      "description": "Fetch Salesforce records (Account, Contact, Lead, Deal, Article, Case) with optional identifier or advanced filtering. Supports single record fetch by ID/field or bulk queries with complex filters, sorting, aggregation, and pagination.",
      "source": "cache",
      "providerConfigKey": "salesforce-ybzg",
      "category": "CRM",
      "parameters": {
        "type": "object",
        "properties": {
          "input": {
            "type": "object",
            "properties": {
              "operation": {
                "type": "string",
                "enum": ["fetch"],
                "description": "The operation to perform. Must be 'fetch'."
              },
              "entityType": {
                "type": "string",
                "enum": ["Account", "Contact", "Lead", "Deal", "Opportunity", "Article", "Case"],
                "description": "The type of Salesforce object to fetch."
              },
              "identifier": {
                "type": "string",
                "description": "Optional: The specific ID or unique field value (like Name or Email based on entityType) to fetch a single record. Use 'all' to fetch multiple records (requires filters or uses defaults)."
              },
              "identifierType": {
                "type": "string",
                "description": "Optional: Specifies which field the 'identifier' value refers to (e.g., 'Id', 'Name', 'Email'). Only needed if 'identifier' is provided and isn't an Id or 'all'."
              },
              "filters": {
                "type": "object",
                "description": "Optional: Advanced criteria for filtering records when fetching multiple. Required if 'identifier' is 'all' or omitted. Supports conditions, sorting, pagination, aggregation, and field selection.",
                "properties": {
                  "conditions": {
                    "type": "array",
                    "items": {
                      "type": "object",
                      "properties": {
                        "field": {
                          "type": "string",
                          "description": "The API name of the field to filter on. IMPORTANT: For fields on related objects, use dot notation, for example: 'Account.Name' or 'CreatedBy.Email'."
                        },
                        "operator": {
                          "type": "string",
                          "enum": ["equals", "notEquals", "greaterThan", "lessThan", "greaterOrEqual", "lessOrEqual", "contains", "notContains", "startsWith", "endsWith", "in", "notIn", "between", "isNull", "isNotNull"],
                          "description": "Comparison operator."
                        },
                        "value": {
                          "type": ["string", "number", "boolean", "array", "object", "null"],
                          "description": "Single value for comparison (can be null)."
                        },
                        "values": {
                          "type": "array",
                          "items": { "type": ["string", "number", "boolean", "null"] },
                          "nullable": true,
                          "description": "Multiple values for 'in', 'notIn', 'between' operators (usually strings, numbers, booleans, or null)."
                        }
                      },
                      "required": ["field", "operator"]
                    },
                    "description": "List of filter conditions to apply to the query."
                  },
                  "logic": {
                    "type": "string",
                    "description": "Logical combination of conditions (e.g., '1 AND (2 OR 3)'). Defaults to AND if multiple conditions exist."
                  },
                  "orderBy": {
                    "type": "array",
                    "items": {
                      "type": "object",
                      "properties": {
                        "field": { "type": "string" },
                        "direction": { "type": "string", "enum": ["ASC", "DESC"] }
                      },
                      "required": ["field"]
                    },
                    "description": "Sorting criteria for results."
                  },
                  "limit": {
                    "type": "number",
                    "description": "Maximum number of records to return (default: 15).",
                    "default": 15,
                    "maximum": 15
                  },
                  "offset": {
                    "type": "number",
                    "description": "Number of records to skip (pagination offset)."
                  },
                  "includeFields": {
                    "type": "array",
                    "items": { "type": "string" },
                    "description": "Specific Salesforce API field names to return. If omitted, default fields are returned."
                  },
                  "excludeFields": {
                    "type": "array",
                    "items": { "type": "string" },
                    "description": "Specific Salesforce API field names to exclude from results."
                  },
                  "timeFrame": {
                    "type": "string",
                    "description": "Predefined time frame for date filtering (e.g., 'last7days', 'thisMonth', 'lastYear')."
                  },
                  "groupBy": {
                    "type": "array",
                    "items": { "type": "string" },
                    "description": "Fields for grouping results (used with aggregate functions)."
                  },
                  "aggregate": {
                    "type": "array",
                    "items": {
                      "type": "object",
                      "properties": {
                        "function": { "type": "string", "enum": ["SUM", "AVG", "COUNT", "MIN", "MAX"] },
                        "field": { "type": "string" },
                        "alias": { "type": "string" }
                      },
                      "required": ["function", "field"]
                    },
                    "description": "Aggregation functions for statistical queries (e.g., COUNT, SUM, AVG)."
                  },
                  "includeDeleted": {
                    "type": "boolean",
                    "description": "Include deleted/archived records in results? Defaults to false."
                  }
                }
              },
              "fields": {
                "type": "array",
                "items": { "type": "string" },
                "description": "Optional: Specific Salesforce API field names to include in the results (alternative to 'filters.includeFields')."
              },
              "format": {
                "type": "string",
                "enum": ["detailed", "simple", "raw"],
                "description": "Response format: 'detailed' (normalized with metadata), 'simple' (minimal data), 'raw' (full Salesforce API response). Defaults to 'detailed'.",
                "default": "detailed"
              }
            },
            "required": ["operation", "entityType"]
          }
        },
        "required": ["input"]
      }
    },
    {
      "name": "create_entity",
      "description": "Create Salesforce entity/entities (Account, Contact, Lead, Deal, Article, Case). SINGLE: Use 'fields' or 'record'. BATCH: Use 'records' array (max 100). Automatically validates fields and returns full created record(s) with metadata for conversational summaries.",
      "source": "action",
      "providerConfigKey": "salesforce-ybzg",
      "category": "CRM",
      "parameters": {
        "type": "object",
        "properties": {
          "input": {
            "type": "object",
            "properties": {
              "operation": {
                "type": "string",
                "enum": ["create"],
                "description": "Always 'create'. Batch mode auto-detected when 'records' array provided."
              },
              "entityType": {
                "type": "string",
                "enum": ["Account", "Contact", "Lead", "Deal", "Opportunity", "Article", "Case"],
                "description": "REQUIRED. The type of Salesforce entity to create."
              },
              "fields": {
                "type": "object",
                "description": "SINGLE RECORD: Use this for creating ONE record. Key-value pairs of Salesforce API field names (e.g., FirstName, LastName, Email, Company, Phone, Title, Status, Rating). Either 'fields' or 'record', not both.",
                "additionalProperties": true
              },
              "record": {
                "type": "object",
                "description": "SINGLE RECORD: Alias for 'fields'. Use this for creating ONE record. Either 'fields' or 'record', not both.",
                "additionalProperties": true
              },
              "records": {
                "type": "array",
                "items": {
                  "type": "object",
                  "additionalProperties": true,
                  "description": "Record object with Salesforce field names and values."
                },
                "description": "BATCH MODE: Use this for creating MULTIPLE records at once (max 100). Array of record objects, each with Salesforce field names. Mutually exclusive with 'fields'/'record'."
              },
              "parentId": {
                "type": "string",
                "description": "Optional parent record ID for creating child records or related entities (e.g., AccountId for creating a Contact under an Account)."
              },
              "idempotencyKey": {
                "type": "string",
                "description": "Optional unique key for this request to prevent duplicate creation. Use consistent format: e.g., 'contact-john-doe-2025-11-20' or 'lead-acme-corp-2025-11-20'."
              },
              "format": {
                "type": "string",
                "enum": ["detailed", "simple", "raw"],
                "description": "Response format: 'detailed' (with warnings and metadata), 'simple' (just success/id), 'raw' (full API response). Defaults to 'detailed'.",
                "default": "detailed"
              }
            },
            "required": ["operation", "entityType"]
          }
        },
        "required": ["input"]
      }
    },
    {
      "name": "update_entity",
      "description": "Update existing Salesforce record(s) (Account, Contact, Lead, Deal, Article, Case). Supports single record updates by ID/field or batch updates with automatic field name mapping and idempotency.",
      "source": "action",
      "providerConfigKey": "salesforce-ybzg",
      "category": "CRM",
      "parameters": {
        "type": "object",
        "properties": {
          "input": {
            "type": "object",
            "properties": {
              "operation": {
                "type": "string",
                "enum": ["update", "batch_update"],
                "description": "The operation to perform. Use 'update' for a single record, 'batch_update' for multiple records."
              },
              "entityType": {
                "type": "string",
                "enum": ["Account", "Contact", "Lead", "Deal", "Opportunity", "Article", "Case"],
                "description": "The type of Salesforce object to update."
              },
              "identifier": {
                "type": "string",
                "description": "Use for a SINGLE record update. The value used to find the record (e.g., a record ID, Name, or Email)."
              },
              "identifierType": {
                "type": "string",
                "enum": ["Id", "Name", "Email", "ExternalId"],
                "description": "The type of field the 'identifier' refers to. Defaults to 'Id' if not provided."
              },
              "fields": {
                "type": "object",
                "minProperties": 1,
                "description": "Use for a SINGLE record update. Key-value pairs of Salesforce API field names and their new values. Only provided fields will be updated.",
                "additionalProperties": true
              },
              "record": {
                "type": "object",
                "description": "Alias for 'fields'. Use for a SINGLE record update. Key-value pairs of Salesforce API field names and their new values.",
                "additionalProperties": true
              },
              "records": {
                "type": "array",
                "items": {
                  "type": "object",
                  "properties": {
                    "identifier": {
                      "type": "string",
                      "description": "The value used to find the record (e.g., a record ID, Name, or Email)."
                    },
                    "identifierType": {
                      "type": "string",
                      "enum": ["Id", "Name", "Email", "ExternalId"],
                      "description": "The type of field the 'identifier' refers to. Defaults to 'Id'."
                    },
                    "fields": {
                      "type": "object",
                      "description": "Key-value pairs of Salesforce API field names and their new values.",
                      "additionalProperties": true
                    },
                    "idempotencyKey": {
                      "type": "string",
                      "description": "Optional unique key for this specific update to prevent duplicate operations."
                    }
                  },
                  "required": ["identifier", "fields"]
                },
                "description": "Use for a BATCH update (max 100 records). An array of objects, each with an 'identifier', 'fields', and optional 'identifierType' and 'idempotencyKey'."
              },
              "idempotencyKey": {
                "type": "string",
                "description": "Optional unique key for this request to prevent duplicate updates. Use consistent format: e.g., 'update-contact-123-2025-11-20' or 'update-lead-acme-2025-11-20'."
              },
              "format": {
                "type": "string",
                "enum": ["detailed", "simple", "raw"],
                "description": "Response format: 'detailed' (with warnings and metadata), 'simple' (just success status), 'raw' (full Salesforce API response). Defaults to 'detailed'.",
                "default": "detailed"
              }
            },
            "required": ["operation", "entityType"]
          }
        },
        "required": ["input"]
      }
    },
    {
      "name": "create_outlook_entity",
      "description": "Create Outlook entities: Message, Event, or Contact. Supports field validation, sanitization, and automatic parent ID handling for nested entities.",
      "source": "action",
      "providerConfigKey": "outlook",
      "category": "Email & Calendar",
      "parameters": {
        "type": "object",
        "properties": {
          "input": {
            "type": "object",
            "properties": {
              "operation": {
                "type": "string",
                "enum": ["create"],
                "description": "Operation must be 'create'."
              },
              "entityType": {
                "type": "string",
                "enum": ["Message", "Event", "Contact"],
                "description": "The type of Outlook entity to create."
              },
              "record": {
                "type": "object",
                "description": "Entity fields. Aliases: 'record' or 'fields'. Field names match Microsoft Graph API names (e.g., subject, body, toRecipients, start, end, displayName, emailAddresses, etc.).",
                "additionalProperties": true
              },
              "fields": {
                "type": "object",
                "description": "Alias for 'record'. Key-value pairs of entity properties.",
                "additionalProperties": true
              },
              "parentId": {
                "type": "string",
                "description": "Optional parent record ID (e.g., folder ID for Message, calendar ID for Event, contact folder ID for Contact)."
              }
            },
            "required": ["operation", "entityType"]
          }
        },
        "required": ["input"]
      }
    },
    {
      "name": "update_outlook_entity",
      "description": "Update Outlook entities: Message, Event, or Contact. Supports field validation and sanitization with patch operations.",
      "source": "action",
      "providerConfigKey": "outlook",
      "category": "Email & Calendar",
      "parameters": {
        "type": "object",
        "properties": {
          "input": {
            "type": "object",
            "properties": {
              "operation": {
                "type": "string",
                "enum": ["update"],
                "description": "Operation must be 'update'."
              },
              "entityType": {
                "type": "string",
                "enum": ["Message", "Event", "Contact"],
                "description": "The type of Outlook entity to update."
              },
              "id": {
                "type": "string",
                "description": "The ID of the entity to update."
              },
              "patch": {
                "type": "object",
                "description": "Fields to update. Only provided fields will be changed.",
                "additionalProperties": true
              },
              "fields": {
                "type": "object",
                "description": "Alias for 'patch'. Key-value pairs of fields to update.",
                "additionalProperties": true
              }
            },
            "required": ["operation", "entityType", "id"]
          }
        },
        "required": ["input"]
      }
    },
    {
      "name": "fetch_outlook_entity",
      "description": "Fetch Outlook entities: Message, Event, Contact, MailFolder, Calendar, or Attachment. Supports ODATA filtering, pagination, and field selection.",
      "source": "cache",
      "providerConfigKey": "outlook",
      "category": "Email & Calendar",
      "parameters": {
        "type": "object",
        "properties": {
          "input": {
            "type": "object",
            "properties": {
              "operation": {
                "type": "string",
                "enum": ["fetch"],
                "description": "Operation must be 'fetch'."
              },
              "entityType": {
                "type": "string",
                "enum": ["Message", "Event", "Contact", "MailFolder", "Calendar", "Attachment"],
                "description": "The type of Outlook entity to fetch."
              },
              "parentId": {
                "type": "string",
                "description": "Optional parent ID (e.g., folder ID for Message, calendar ID for Event, message ID for Attachment). Required for Attachment."
              },
              "filters": {
                "type": "object",
                "description": "Optional ODATA filters for querying multiple entities.",
                "properties": {
                  "from": { "type": "string", "description": "Filter by sender (for Message)." },
                  "subject": { "type": "string", "description": "Filter by subject text (startswith)." },
                  "isRead": { "type": "boolean", "description": "Filter by read status (Message)." },
                  "hasAttachments": { "type": "boolean", "description": "Filter by attachment presence (Message)." },
                  "displayName": { "type": "string", "description": "Filter by name (for Contact, MailFolder)." },
                  "limit": { "type": "integer", "description": "Maximum records to return (max 1000)." },
                  "offset": { "type": "integer", "description": "Number of records to skip (pagination)." }
                }
              },
              "format": {
                "type": "string",
                "enum": ["detailed", "simple", "raw"],
                "description": "Response format: 'detailed' (normalized), 'simple' (minimal), or 'raw' (API response). Defaults to 'detailed'.",
                "default": "detailed"
              }
            },
            "required": ["operation", "entityType"]
          }
        },
        "required": ["input"]
      }
    },
    {
      "name": "fetch_outlook_event_body",
      "description": "Fetch the body content of an Outlook event. Returns event subject, body content, and content type (text or HTML).",
      "source": "cache",
      "providerConfigKey": "outlook",
      "category": "Email & Calendar",
      "parameters": {
        "type": "object",
        "properties": {
          "input": {
            "type": "object",
            "properties": {
              "operation": {
                "type": "string",
                "enum": ["fetch"],
                "description": "Operation must be 'fetch'."
              },
              "eventId": {
                "type": "string",
                "description": "The ID of the Outlook event to fetch body from."
              },
              "calendarId": {
                "type": "string",
                "description": "Optional calendar ID. If omitted, uses default calendar."
              }
            },
            "required": ["operation", "eventId"]
          }
        },
        "required": ["input"]
      }
    },
    {
      "name": "create_internal_email_draft",
      "description": "Create durable internal email draft artifacts in ASO.",
      "source": "internal",
      "providerConfigKey": "__DESKTOP__",
      "category": "Desktop",
      "parameters": {
        "type": "object",
        "properties": {
          "input": {
            "type": "object",
            "properties": {
              "operation": { "type": "string", "enum": ["create"] },
              "artifactSpecId": { "type": "string" },
              "artifactSpec": { "type": "object" },
              "sourceData": {}
            },
            "required": ["operation", "artifactSpecId", "artifactSpec", "sourceData"]
          }
        },
        "required": ["input"]
      }
    },
    {
      "name": "generate_file",
      "description": "Generate a durable PDF, XLSX, or DOCX artifact from workflow data.",
      "source": "desktop",
      "providerConfigKey": "__DESKTOP__",
      "category": "Desktop",
      "parameters": {
        "type": "object",
        "properties": {
          "input": {
            "type": "object",
            "properties": {
              "operation": { "type": "string", "enum": ["generate"] },
              "format": { "type": "string", "enum": ["xlsx", "docx", "pdf"] },
              "title": { "type": "string" },
              "template": { "type": "string" },
              "artifactSpecId": { "type": "string" },
              "artifactSpec": { "type": "object" },
              "data": {}
            },
            "required": ["operation", "format", "title", "artifactSpecId", "artifactSpec"]
          }
        },
        "required": ["input"]
      }
    }
  ],
  "beats": [],
  "providers": {
    "google-mail": {
      "endpoint": "gmail",
      "provider_config_key": "google-mail",
      "connection_id": "e5bce133-e6df-4cb7-9fb1-ec1d48023079",
      "objects": ["Message"]
    },
    "google-calendar": {
      "endpoint": "calendar",
      "provider_config_key": "google-calendar",
      "connection_id": "30009351-89b2-4546-9367-bce987d6d79d",
      "objects": ["Calendar", "Event"]
    },
    "notion": {
      "endpoint": "notion",
      "provider_config_key": "notion",
      "connection_id": "notion-connection-id-placeholder",
      "objects": ["Page", "Database"]
    },
    "salesforce-2": {
      "endpoint": "salesforce",
      "provider_config_key": "salesforce-2",
      "connection_id": "2afdea8f-9c5a-4555-9e88-6c440e59c037",
      "objects": ["Account", "Contact", "Lead", "Deal", "Article", "Case"]
    },
    "outlook": {
      "endpoint": "outlook",
      "provider_config_key": "outlook",
      "connection_id": "outlook-connection-id",
      "objects": ["Message", "Event", "Contact", "MailFolder", "Calendar", "Attachment"]
    }
  }
}
