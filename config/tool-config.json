{
  "tools": [
    {
      "name": "request_missing_parameters",
      "description": "Use this tool ONLY when required parameters for another tool are missing.",
      "providerConfigKey": "__META__",
      "category": "Meta",
      "parameters": {
        "type": "object",
        "properties": {
          "intended_tool_name": {
            "type": "string",
            "description": "The exact name of the tool the user likely intended to use."
          },
          "missing_params": {
            "type": "array",
            "items": { "type": "string" },
            "description": "A list of the REQUIRED parameters that are missing."
          },
          "clarification_question": {
            "type": "string",
            "description": "A clear question to ask the user to obtain the missing parameters."
          }
        },
        "required": [
          "intended_tool_name",
          "missing_params",
          "clarification_question"
        ]
      }
    },
    {
      "name": "fetch_emails",
      "description": "Fetch Gmail emails with advanced filtering options.",
      "providerConfigKey": "google-mail",
      "category": "Email",
      "parameters": {
        "type": "object",
        "properties": {
          "operation": {
            "type": "string",
            "enum": ["fetch"],
            "description": "Specifies the operation, always 'fetch'."
          },
          "filters": {
            "type": "object",
            "description": "Optional: Criteria to filter emails.",
            "optional": true,
            "properties": {
              "sender": { "type": "string", "description": "Filter by sender's email.", "optional": true },
              "recipient": { "type": "string", "description": "Filter by recipient's email.", "optional": true },
              "dateRange": { "type": "object", "description": "Filter by date range.", "optional": true, "properties": { "after": { "type": "string" }, "before": { "type": "string" } } },
              "hasAttachment": { "type": "boolean", "description": "Filter for emails with attachments.", "optional": true }
            }
          }
        },
        "required": ["operation"] 
      }
    },
    {
      "name": "send_email",
      "description": "Send an email via Gmail.",
      "providerConfigKey": "google-mail",
      "category": "Email",
      "parameters": {
        "type": "object",
        "properties": {
          "operation": { "type": "string", "enum": ["send"], "description": "Operation must be 'send'." },
          "to": { "type": "string", "description": "The recipient's email address." },
          "subject": { "type": "string", "description": "The subject line of the email." },
          "body": { "type": "string", "description": "The main content/body of the email." }
        },
        "required": ["operation", "to", "subject", "body"]
      }
    },
    {
      "name": "fetch_entity",
      "description": "Fetch Salesforce records with advanced filtering.",
      "providerConfigKey": "salesforce-2",
      "category": "CRM",
      "parameters": {
        "type": "object",
        "properties": {
          "operation": { "type": "string", "enum": ["fetch"], "description": "Operation must be 'fetch'." },
          "entityType": {
            "type": "string",
            "enum": ["Account", "Contact", "Lead", "Deal", "Article", "Case"],
            "description": "The type of Salesforce object to fetch."
          },
          "identifier": {
            "type": "string",
            "description": "Optional: ID or unique value (Name/Email) to fetch a single record. Use 'all' for multiple records.",
            "optional": true
          },
          "filters": {
            "type": "object",
            "description": "Optional: Advanced criteria for filtering records.",
            "optional": true,
            "properties": {
              "conditions": { "type": "array", "items": { "type": "object" }, "description": "List of filter conditions." },
              "logic": { "type": "string", "optional": true, "description": "Logical combination of conditions (e.g., '1 AND 2')." },
              "limit": { "type": "number", "optional": true, "description": "Maximum number of records." }
            }
          }
        },
        "required": ["operation", "entityType"]
      }
    },
    {
      "name": "create_entity",
      "description": "Create a new Salesforce record.",
       "providerConfigKey": "salesforce-2",
       "category": "CRM",
      "parameters": {
        "type": "object",
        "properties": {
          "operation": { "type": "string", "enum": ["create"], "description": "Operation must be 'create'." },
          "entityType": { "type": "string", "enum": ["Account", "Contact", "Lead", "Deal", "Article", "Case"], "description": "Type of Salesforce object to create." },
          "fields": {
            "type": "object",
  "description": "A key-value object of fields for the new record. The required fields depend on the 'entityType'.\n- For a 'Lead', you MUST include 'Company', 'LastName', and 'Status'. Example: { \"Company\": \"Innovate Corp\", \"LastName\": \"Doe\", \"Status\": \"Open - Not Contacted\" }\n- For a 'Contact', you MUST include 'LastName'. Example: { \"LastName\": \"Smith\", \"FirstName\": \"Jane\" }"
          }
        },
        "required": ["operation", "entityType", "fields"] 
      }
    },
        {
      "name": "create_zoom_meeting",
      "description": "Schedules a new meeting in Zoom. Use this for creating calendar events, appointments, or meetings.",
      "providerConfigKey": "zoom",
      "category": "Calendar",
      "parameters": {
        "type": "object",
        "properties": {
          "topic": {
            "type": "string",
            "description": "The title or topic of the meeting."
          },
          "type": {
            "type": "string",
            "description": "The type of meeting.",
            "enum": ["scheduled", "instant", "recurring", "recurringNoFixed"]
          },
          "start_time": {
            "type": "string",
            "description": "The meeting's start time in ISO 8601 format (e.g., '2025-08-14T10:00:00Z'). Required for 'scheduled' and 'recurring' types."
          },
          "duration": {
            "type": "number",
            "description": "The meeting's duration in minutes."
          },
          "timezone": {
            "type": "string",
            "description": "The timezone for the meeting, e.g., 'Africa/Johannesburg'."
          },
          "agenda": {
            "type": "string",
            "description": "A description or agenda for the meeting."
          },
          "recurrence": {
            "type": "object",
            "description": "Recurrence details for a 'recurring' meeting.",
            "properties": {
              "type": {
                "type": "string",
                "enum": ["daily", "weekly", "monthly"]
              },
              "repeat_interval": { "type": "number" },
              "weekly_days": {
                "type": "string",
                "enum": ["sunday", "monday", "tuesday", "wednesday", "thursday", "friday", "saturday"]
              },
              "end_times": {
                "type": "number",
                "description": "How many times the meeting should recur."
              }
            }
          }
        },
        "required": ["topic", "type"]
      }
    },
    {
      "name": "update_entity",
      "description": "Update existing Salesforce record(s).",
      "providerConfigKey": "salesforce-2",
      "category": "CRM",
      "parameters": {
        "type": "object",
        "properties": {
          "operation": { "type": "string", "enum": ["update"], "description": "Operation must be 'update'." },
          "entityType": { "type": "string", "enum": ["Account", "Contact", "Lead", "Deal", "Article", "Case"], "description": "Type of Salesforce object to update." },
          "identifier": { "type": "string", "optional": true, "description": "Optional: ID or unique field value to identify a SINGLE record." },
          "filters": { "type": "object", "optional": true, "description": "Optional: Filters to identify MULTIPLE records." },
          "fields": {
            "type": "object", "minProperties": 1,
            "description": "Required: Key-value pairs of fields and their new values."
          }
        },
        "required": ["operation", "entityType", "fields"]
      }
    }
  ],
  "beats": [],
  "providers": {
    "google-mail": {
      "endpoint": "gmail",
      "provider_config_key": "google-mail",
      "connection_id": "e5bce133-e6df-4cb7-9fb1-ec1d48023079",
      "objects": ["Message"]
    },
    
    "zoom": {
  "endpoint": "zoom",
  "provider_config_key": "zoom",
  "connection_id": "<YOUR_ZOOM_CONNECTION_ID>",
  "objects": ["Meeting"]
},
    "salesforce-2": {
      "endpoint": "salesforce",
      "provider_config_key": "salesforce-2",
      "connection_id": "2afdea8f-9c5a-4555-9e88-6c440e59c037",
      "objects": ["Account", "Contact", "Lead", "Deal", "Article", "Case"]
    }
  }
}